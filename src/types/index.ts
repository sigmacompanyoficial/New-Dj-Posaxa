export interface Product {
  id: string;
  name: string;
  price: number;
  image: string;
}

export interface NavLink {
  label: string;
  href: string;
}

export interface Review {
  text: string;
  author: string;
}
